function FindProxyForURL(url, host) {
	if (
		dnsDomainIs(host, "rutracker.org") ||
		dnsDomainIs(host, "linkedin.com") ||
		dnsDomainIs(host, "lurkmore.to")
	) {
		return "SOCKS 192.168.1.100:9050";
	} else {
		return "DIRECT";
	}
}
#!/bin/bash
ARGUMENTS=$(xrectsel "--x=%x --y=%y --width=%w --height=%h") || exit -1
path=~/tmp/image-$(date --iso-8601=s).gif
byzanz-record --cursor=false --exec="sleep 5m" ${ARGUMENTS} $path
printf $path | xsel -ib
notify-send -u 'normal' 'Screencast-rect' 'Finish'

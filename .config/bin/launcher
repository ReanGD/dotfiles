#!/bin/sh

if pgrep -f "ulauncher"; then
	dbus-send \
	    --session \
	    --print-reply \
	    --dest=net.launchpad.ulauncher \
	    /net/launchpad/ulauncher \
	    net.launchpad.ulauncher.toggle_window
else
	ulauncher&
fi
